let
  problem2 : Chan (?Int. ?String. term) -[ u 1 ]-> Unit
  problem2 c =
    let y : Chan (?Int. return)
        y = &c in
    let g : String -[ p 1 ]-> Unit
        g x = x + recv &c ; term c in     #(* c : ?String. Close *)
    let _ = recv y in
    g ""
in
  unit
